package main

// Supported content types for ByContentType function
const (
	JsonContentType       string = "application/json"
	XmlContentType        string = "application/xml"
	MultipartContentType  string = "multipart/form-data"
	UrlEncodedContentType string = "application/x-www-form-urlencoded"
)
